workflows:
  ios-browserstack-workflow:
    name: iOS Build for BrowserStack
    max_build_duration: 60
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - name: Build Flutter iOS IPA for Cloud Testing
        script: |
          flutter build ipa --no-codesign
    artifacts:
      - build/ios/ipa/*.ipa
